import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import Navigation from "@/components/navigation"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Tamil Nadu MGNREGA Job Card Visualizer",
  description:
    "Comprehensive analysis of MGNREGA implementation across all districts of Tamil Nadu with detailed visualizations and insights",
  keywords:
    "Tamil Nadu, MGNREGA, job cards, rural employment, data visualization, government schemes, districts analysis",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Navigation />
        <main className="min-h-screen bg-gray-50">{children}</main>
      </body>
    </html>
  )
}
